# Security

The project treats command injection, path traversal, host-key changes, output exhaustion, secret leakage, approval replay, malformed remote output, and symlink races as first-class threats.
