# Security Policy

Do not report secrets in public issues. Use GitHub private vulnerability reporting when enabled.

OpsHaven intentionally does not provide arbitrary shell execution, agent-supplied commands, generic file reads, Docker exec, SQL execution, or public network exposure by default.
